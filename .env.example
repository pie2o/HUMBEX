# .env.example — NE PAS mettre de vraies valeurs ici.
# Copie ce fichier en .env sur ta machine (Working Copy) et remplace les placeholders par TES vraies valeurs.
# Ne commite jamais le fichier .env contenant des secrets.

# ----------------------------
# Database (backend / worker)
# ----------------------------
# Chaîne de connexion Postgres (utilisée par le backend / worker).
# Format : postgresql://postgres:YOUR_DB_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres
DATABASE_URL="postgresql://postgres:YOUR_DB_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres"

# ----------------------------
# Supabase (frontend / client)
# ----------------------------
# URL publique du projet Supabase (visible dans Settings → API → Project URL)
NEXT_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"

# Clef publique (anon) pour supabase-js côté client
NEXT_PUBLIC_SUPABASE_ANON_KEY="public-anon-key"

# ----------------------------
# Supabase (server / CI) — TRÈS SENSIBLES
# ----------------------------
# Clef de rôle service (utiliser seulement côté serveur / CI)
SUPABASE_SERVICE_ROLE_KEY="service-role-key"

# Référence du projet Supabase (project ref)
SUPABASE_REF="your-project-ref"

# Jeton personnel pour le Supabase CLI (pour GitHub Actions)
SUPABASE_ACCESS_TOKEN="personal-access-token"

# ----------------------------
# Application / Sécurité
# ----------------------------
# Secret HMAC partagé pour vérifier les webhooks TradingView
TRADINGVIEW_SECRET="your_tradingview_hmac_secret"

# Clé hex (32 bytes) utilisée pour chiffrer les clés API (AES-GCM)
ENCRYPTION_KEY_HEX="32_byte_hex_string_here"

# ----------------------------
# Exchange (test/demo keys)
# ----------------------------
# Tu as choisi d'utiliser Binance demo keys au lieu de Bybit testnet
BINANCE_API_KEY="demo_binance_api_key"
BINANCE_API_SECRET="demo_binance_api_secret"

# ----------------------------
# Options / Flags (optionnel)
# ----------------------------
# Port du backend (ex : 8000)
BACKEND_PORT="8000"

# Intervalle de polling du worker (secondes)
WORKER_POLL_INTERVAL="5"

# Mode test/dry-run pour CCXT (true/false)
CCXT_TEST_MODE="true"

# ----------------------------
# Notes
# ----------------------------
# - Copie ce fichier en .env sur ta machine (Working Copy) et remplace les valeurs par les vraies.
# - Ne pousse jamais .env sur GitHub.
# - Les secrets pour GitHub Actions doivent être ajoutés dans
#   Settings → Secrets and variables → Actions (avec les mêmes noms que ci‑dessus).
